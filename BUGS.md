# Rejestr Błędów (BUGS.md)

| ID | Priorytet | Obszar | Opis problemu | Reprodukcja | Rozwiązanie | Status |
|---|---|---|---|---|---|---|
| BUG-001 | P1 | Dane JSON / Walidacja | `validate_json.py` oraz `UPISJsonDataSubsystem::ReloadAndValidate()` sprawdzają jedynie pola najwyższego poziomu o sufiksie `_id`. Nie sprawdzają referencji wewnątrz list/zagnieżdżonych struktur (`location_id` w harmonogramach/spawnach, `item` w tabelach lootu, `target_id` i `item_ids` w questach, `speaker_id` i akcji w dialogach). | Uruchomienie `validate_json.py` nie wykrywa braku referencji dla zagnieżdżonych pól. | Rozszerzono `validate_json.py` oraz `UPISJsonDataSubsystem.cpp` o pełną walidację grafów referencji zagnieżdżonych oraz kontrolę kompletności pól domeny (`swords`, `bows`, `armors`, `monsters`, `spells`, `schedules`, `quests`). | NAPRAWIONY (ZAMKNIĘTY) |
| BUG-002 | P1 | Dane JSON / Balans | Brak atrybutów statystycznych w plikach `items_weapons_swords.json`, `items_weapons_bows.json`, `items_armors.json` i `spells.json` (`damage`, `required_strength`, `required_dexterity`, `defense`, `cast_mana`). Formuły w `PISProgressionLibrary` wymagają bazowych obrażeń i wymagań statystyk. | Sprawdzenie rekordów np. `sword_01`, `bow_01`, `armor_straz_podstawowa` w JSON wykazuje brak pól bojowych. | Uzupełniono wszystkie 20 mieczy, 10 łuków, 4 zbroje i 2 czary o zbalansowane atrybuty statystyczne (`damage`, `defense`, `required_strength`, `required_dexterity`, `mana`, `cast_time`). | NAPRAWIONY (ZAMKNIĘTY) |
| BUG-003 | P1 | Questy i Dialogi | Zadania kandydackie (`old_trial_1..5`, `new_trial_1..5`) oraz poboczne (`side_1..10`) posiadają skopiowane atrapy tekstów (`"Zbierz ślad i wróć."`) bez struktury `objectives` i etapów. Plik `dialogues_prototype.json` zawiera tylko 1 dialog dla 1 NPC. Brak dialogów dla obozów, nauczycieli, handlarzy oraz finalnego wyboru frakcji i epilogu. | Wczytanie plików `quests_*.json` i `dialogues_prototype.json` potwierdza brak rozgałęzień dialogowych dla 65 NPC i brak mechanizmu wyboru frakcji. | Zastąpiono atrapy rozbudowanymi zadaniami z celami (`objectives`). Utworzono 8 pełnych drzew dialogowych w `dialogues_prototype.json` dla obu obozów, nauczycieli, handlu i wyboru frakcji z epilogiem. | NAPRAWIONY (ZAMKNIĘTY) |
| BUG-004 | P1 | NPC Schedules / Lokacje | Harmonogramy NPC w `npc_schedules.json` odwołują się do markerów (`work_stary_porzadek`, `patrol_stary_porzadek`, `bed_npc_sta_01`), które nie są zmapowane lub połączone z rekordami w `world_locations.json`. Fallback (`teleport_out_of_sight`) nie wskazuje bezpiecznego punktu lokacji. | Porównanie `npc_schedules.json` z `world_locations.json`. | Zunifikowano mapowanie `location_id` we wszystkich 65 harmonogramach (`loc_1` dla Starego Obu, `loc_2` dla Nowego, `loc_3..loc_7` dla reszty) oraz dodano rekord `teleport_out_of_sight` jako `fallback` do `world_locations.json`. | NAPRAWIONY (ZAMKNIĘTY) |
| BUG-005 | P1 | Potwory / Spawny | Rekordy w `monsters.json` (`monster_1..6`) nie zawierają statystyk HP, obrażeń, XP, łupu ani przypisanego trofeum przy skórowaniu. | Sprawdzenie `monsters.json` wykazuje wyłącznie `id`, `name`, `biome`, `behavior`. | Uzupełniono wszystkie 6 gatunków potworów o statystyki `hp`, `damage`, `xp`, `trophy_id` oraz przypisano zbalansowane tabele lootu (`loot_monster_1..6`) do `loot_tables.json`. | NAPRAWIONY (ZAMKNIĘTY) |
| BUG-006 | P1 | Logika C++ (Quests, Chests, Clock, Inventory) | 1) `PISQuestComponent::FinishQuest` pozwala na wielokrotne kończenie i brak weryfikacji wykluczenia frakcji.<br>2) `PISLockedChest::SubmitLockTurn` nie sprawdza, czy gracz ma co najmniej 1 wytrych przed próbą otwarcia.<br>3) `PISWorldClock::SleepTo(TargetHour)` nie zwiększa `Day` przy śnie przez północ.<br>4) `PISInventoryComponent::EquipWeapon` nie weryfikuje wymagań statystyk broni. | Analiza kodu w `Source/PopiolISol/Private/*.cpp`. | Poprawiono logikę w C++ (`PISQuestComponent.cpp` z weryfikacją wykluczenia i idempotencji, `PISLockedChest.cpp` ze sprawdzaniem wytrychów, `PISWorldClock.cpp` ze snem przez północ, `PISInventoryComponent.cpp` ze sprawdzaniem statystyk). | NAPRAWIONY (ZAMKNIĘTY) |
